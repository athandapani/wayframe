{
  "t1": { "status": "done", "note": "RoadmapData schema discipline: .strict() everywhere, drift fixes, integer schemaVersion + migration ladder, localStorage validated through the same pipeline as file-open." },
  "t11": { "status": "done", "note": "Portfolio envelope & identity — RoadmapData split into Portfolio + Program (id/portfolioId/order), PortfolioDocument as the new root shape, RenderableProgram/mergeForRender keeps the render layer Portfolio-agnostic." },
  "t2": { "status": "done", "note": "Doctrine-only ticket (#76), no code shape of its own — resolution already fully written as prose in CONTEXT.md's 'Document content vs. viewer preference' section. Becomes actionable via t8/t18/t19/t22." },
  "t8": { "status": "done", "note": "Color & preference inventory — confirmed 28 wayframe:* keys and ~10 hardcoded color sites. Wired theme.todayColor/annotationColor/tooltipBg+Ink/ragColor and fixed two markerHalo bypasses; ragColor published via --wf-rag-* CSS vars for ExecutiveView/ExecutiveTimeline." },
  "t3": { "status": "done", "note": "Turso/libSQL selected — sub-20ms embedded-replica reads, ~1GB row limit (fits inline base64 assets), free branching (D1 ruled out for its 2MB cap). Scaffolded src/lib/db/client.ts: server-only singleton, TURSO_DATABASE_URL/TURSO_AUTH_TOKEN via env-guard, falls back to a local libSQL file (.data/wayframe-dev.db) in dev/test so t12/t40 can build on a real client with no live Turso project required until deploy." },
  "t5": { "status": "done", "note": "Auth.js v5 (next-auth@beta) + JWT sessions, Google provider requesting only openid/email/profile at sign-in. src/lib/auth/auth.ts (NextAuth config, AUTH_GOOGLE_ID/SECRET/AUTH_SECRET via env-guard, dev-only secret fallback since Auth.js requires one unconditionally), src/app/api/auth/[...nextauth]/route.ts (handlers), src/lib/auth/scopes.ts + request-slides-access.ts (incremental-OAuth helper for Slides/Drive scope, for t30 to call at export time, not sign-in — verified end-to-end against a live dev server that the base and incremental-scope authorize URLs both come out correct). Also added tursoConfigured/googleAuthConfigured to /api/health." },
  "t6": { "status": "done", "note": "Google Slides API fidelity probe — resolved by gist only, no code artifact: strong native fidelity (stars/squares/circles/diamonds, 270° rotation, text runs, elbow/curved connectors, grouping); only flag/pennant missing, resolved via a composed wave-shape + rectangle staff (#80 follow-up). Feeds t28's Deck IR schema design — no scaffold built yet since there's no runtime surface for a probe ticket to land in; detailed shape/feature matrix deferred to whenever t28 actually starts." },
  "t4": { "status": "done", "note": "Yjs + PartyKit (now y-partyserver) stays, but the resolution was corrected: cloud-prem deploy to your own free Cloudflare account, not the old ~$50/mo hosted partykit.dev platform — PartyKit was acquired by Cloudflare (Apr 2024, now open source, ISC) and Durable Objects went free on the Workers Free plan (Apr 2025). Also found subdocuments don't nest past one level, so Program/Scenario both attach as direct subdocs of one Portfolio doc, not truly 3-deep — noted for t14. Scaffolded party/ (separate Cloudflare Worker package: PortfolioRoom extends YServer, wrangler.jsonc DO binding, onLoad/onSave stubbed for t12) and src/lib/realtime/provider.ts (client-side y-partyserver/provider wrapper, unconsumed until t12/t14). Verified for real: wrangler dev boots the DO locally and a live two-client Yjs sync test (separate Y.Docs, real WebSocket, real CRDT merge) converged correctly." }
}
