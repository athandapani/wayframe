[
{"key":"t1","type":"grilling","title":"Schema discipline: SSoT, strictness, versioning, migration contract","question":"Do TypeScript types generate from Zod (or vice versa)? Does the document schema become strict so unknown-key drops are loud instead of silent? What is the versioning + migration contract that replaces today's decorative `schemaVersion` (currently zero comparison sites, no migration code anywhere)? Must resolve three known drifts between `types.ts` and `document-file.ts`'s `RoadmapDataSchema`: `owner` (required in TS, optional in Zod), `actionItems` (typed `ActionItem[]` in TS, `z.unknown()` in Zod), and `annotation.message` (required in TS, optional in Zod). Also covers the unvalidated `as RoadmapData` cast on the localStorage load path.","blockers":[],"gist":"TS stays SSoT (JSDoc kept) with a compile-time Zod↔TS assertion; every schema object goes `.strict()`; `schemaVersion` becomes a monotonic integer with a migrate-then-validate-once ladder; localStorage now validates through the same pipeline as file-open."},
{"key":"t2","type":"grilling","title":"Document-content vs viewer-preference doctrine, restated for multi-user","question":"What is the general RULE (not a case-by-case list) deciding whether a knob lives in the document (travels, shared, exported, visible to every collaborator) versus the viewer (local, per-person)? The codebase today has a deliberate split with 25+ global localStorage viewer-preference keys; Style Override, Theme, Snapshot, and swimlane visibility all cross that line intentionally under the new multi-user model, and each downstream ticket will otherwise invent its own rule. Must explicitly address what happens when one collaborator's document-content change (e.g. hiding a lane, switching the theme) is visible live to every other viewer mid-session.","blockers":[],"gist":"Rule: document content if the change should be true of the plan for every collaborator (live) and every future viewer; viewer preference if it only affects my own screen right now. Consequences: content changes live-sync like any other CRDT edit (no bespoke notification, rely on presence UI), no per-viewer override layer exists, and Saved Views narrow to true viewer-prefs only (built-in presets lose their theme clauses)."},
{"key":"t3","type":"research","title":"Hosted SQLite platform selection","question":"Which hosted SQLite option (Turso/libSQL, Cloudflare D1, or a Postgres-as-fallback on Vercel) fits Next.js 16 on Vercel — covering serverless cold-start latency, row/blob size limits (today's `companyLogo.dataUrl` and `attachments[].url` are inline base64 data URLs), branching/preview-environment support, and cost at showcase scale? Report a recommendation with tradeoffs, not just an inventory.","blockers":[],"gist":"Turso/libSQL — sub-20ms cold-start reads via embedded replicas, ~1GB row limit fits inline base64 assets, free branching for Vercel PR previews. D1 ruled out (2MB row cap)."},
{"key":"t4","type":"research","title":"Realtime stack: CRDT library + WebSocket/Durable-Object host","question":"Compare Yjs, Loro, and Automerge for this document shape, and identify where the persistent socket lives given Vercel serverless functions cannot hold a long-lived WebSocket (Partykit, Cloudflare Durable Objects, Liveblocks, Ably, or self-hosted). Must report: subdocument support (for Portfolio → Program → Scenario nesting), origin-scoped UndoManager semantics, awareness/presence API shape, and the server-side persistence hook (how CRDT updates reach the hosted DB from #t3).","blockers":[],"gist":"Yjs + Partykit — native subdocument support fits Portfolio→Program→Scenario nesting; ~$50/mo. Automerge ruled out (undo is local-only, unworkable for multi-user)."},
{"key":"t5","type":"research","title":"Google identity & OAuth on Next.js 16","question":"Auth.js v5 vs. a hand-rolled OAuth flow on Next.js 16's actual conventions (this version diverges from training data — read `node_modules/next/dist/docs/` before concluding). Where does the session live, and are Slides/Drive scopes requested at initial sign-in or via incremental authorization (affects consent-screen friction for a public portfolio showcase)?","blockers":[],"gist":"Auth.js v5, JWT sessions to start. Incremental OAuth — request Slides/Drive scope only at export time, not sign-in, to dodge the unverified-app warning."},
{"key":"t6","type":"research","title":"Google Slides API fidelity probe","question":"Which shape/text/line features does `presentations.batchUpdate` actually support: the autoshape catalogue (does it cover star/flag/square/rectangle/circle/diamond?), text-run styling, shape rotation (needed for 270°-rotated Swimlane Group labels), dash patterns, line/connector primitives, and grouping? Establish the Slides-side fidelity floor for the shared Deck model.","blockers":[],"gist":"Strong native fidelity — stars, squares, circles, diamonds, 270° rotation, text runs, elbow/curved connectors, grouping all native. Only flag/pennant shape missing (resolved via a composed `wave` shape + rectangle staff — see #80 follow-up comment)."},
{"key":"t7","type":"research","title":"pptxgenjs native-shape fidelity probe","question":"Which parts of the render vocabulary map to native PPTX autoshapes and text via `pptxgenjs`: diamond/star/flag/square/rectangle/circle markers, round-capped pill vs. rectangle phases, leader lines, rotated labels, the BLUF panel's sanitized rich-text HTML, and elbow/s-curve/rounded connectors? Report what degrades or must still rasterize.","blockers":[],"gist":"Shapes strong (flag missing, matches Slides — same `wave`+rectangle fix, see #81 follow-up). Connectors are straight-only — no bent/curved native support. BLUF rich-text HTML needs pre-splitting into runs."},
{"key":"t8","type":"task","title":"Color & preference inventory","question":"Produce an exhaustive list of every hardcoded color in the render path (`RoadmapTimeline.tsx`, `ChartLegend.tsx`, ghost badges, tooltip, today-line, annotation line — roughly 10 known sites) and every one of the ~28 `wayframe:*` localStorage keys, each classified as theme token / document content / viewer preference per the rule from #t2. Pure inventory — the classification rule itself is #t2's job, not this ticket's.","blockers":["t2"],"gist":"7 hardcoded-color groups need new/wired-up theme tokens (todayColor, annotationColor, tooltipBg, ragColor family, plus wiring the existing but underused markerHalo). Of the 28 localStorage keys, only `wayframe:theme` flips to document content — the other 25 stay viewer preferences; `document`/`edit-lock` are superseded by hosted DB/realtime collab."},
{"key":"t9","type":"task","title":"Delta-annotation inventory & tier budget","question":"Document precisely what the two existing ghost systems draw today (`originalDate` slip badges: badge or dashed-outline styles, delta-only label text; `potentialDate` at-risk projections: sibling/comet/zone styles, \"+Nd risk\" label) and exactly how much vertical tier space exists above a marker (`GHOST_TIER_DY = [-18,-30,-70]`, consumed independently by both systems today). A scenario-diff ghost would be a third consumer of the same three slots — quantify the actual contention before #t23 designs the unification.","blockers":[],"gist":"Slip-badge and at-risk-projection each run an independent `layoutGhostBadges` pass over the same 3 tiers (12px/40px gaps), seeded only with title blockers, never with each other — a milestone with both a slip ghost and a near-term at-risk projection can get two badges at the same tier-0 slot. A third (scenario-diff) consumer would face the same blind spot; real fix needs one shared placement pass, not three independent ones."},
{"key":"t10","type":"prototype","title":"Zoom & fit-to-screen mechanism","question":"Is \"zoom to a specific timeframe\" a viewport/scale transform over the full computed date domain, or a recomputation of the date domain itself? The latter re-runs every collision pass and changes label density each zoom step; the former makes text/label scale wrong as you zoom. No zoom concept exists today — the timeline renders its full computed domain in an `overflow-x-auto` container with zero windowing.","blockers":[],"gist":"Hybrid: live CSS scaleX() preview during the gesture, debounced commit to a real filtered-domain recompute. Pure transform distorts markers into ellipses/rhombi and can never re-flow a crushed label cluster at any zoom level. Recompute is correct but needs an explicit domainOverride — computeDomain's own ±14-day content-derived pad silently floors how tight a requested window can render. Controls: two-thumb range slider + zoom +/-, not preset widths (revised after feedback)."},

{"key":"t11","type":"grilling","title":"Portfolio envelope & identity","question":"Define the top-level document shape: how a Portfolio owns many Programs plus a Baseline, a set of Scenarios, a set of Snapshots, and a Theme; what carries identity and ordering; and what today's flat `RoadmapData` becomes once a Program gains its own identity. This is the critical-path chokepoint of the whole map — nearly everything else depends on it, directly or transitively.","blockers":["t1"],"gist":"No stored `baseline` field — it's each Program's own live data with no Scenario applied. Program gets a stable id + portfolioId (independently addressable, not embedded) + explicit order. schemaVersion, legendCategories, and companyLogo move up to Portfolio (shared across the merged All-Programs view); generatedAt/lastUpdatedAt stay per-Program. Snapshots are append-only/undeletable; Scenarios have no order field; which Scenario a viewer is looking at is viewer-local, never persisted."},

{"key":"t12","type":"grilling","title":"Storage granularity & read/write topology","question":"What is one row in the hosted DB: a whole-Portfolio blob, a per-Program blob, an append-only CRDT update log, or normalized tables? How does the All-Programs view read N programs cheaply while the realtime server (#t4) is writing continuously to the same data?","blockers":["t3","t4","t11"],"gist":"One row per Program is the unit (a Yjs-snapshot blob keyed by program id), not a whole-Portfolio blob or normalized tables. A separate append-only program_updates log is the realtime write target (Yjs updates), periodically compacted into the Program row's snapshot. All-Programs reads hit only the small per-Program snapshot rows for a portfolio_id — never the update log — so the two paths share no hot row and need no special isolation."},
{"key":"t13","type":"prototype","title":"Scenario representation & rebase","question":"Is a Scenario a sparse delta list layered over Baseline, or a fully materialized copy? What happens to a delta when the Baseline milestone or phase it targets is concurrently modified or deleted underneath it — rebase silently, orphan the delta, or surface a conflict to the user? Scenario deltas can add, remove, or modify milestones AND phases, so a Baseline deletion is a real case to design for, not an edge case to hand-wave.","blockers":["t11"],"gist":"Sparse delta list: `overrides` (keyed by Baseline item id: modify-patch or remove) plus `additions` (scenario-only new items), resolved live against Baseline's current state every time — never a materialized copy. Same-field conflicts let the override win with an informational \"plan moved\" note (via a per-item `rev` bumped on Baseline edits). A Baseline deletion under a live `modify` override surfaces as a persistent, dismiss-only \"orphaned\" conflict — never silently dropped or resurrected. A Baseline deletion under an already-agreeing `remove` produces no conflict and is quietly garbage-collected."},
{"key":"t18","type":"prototype","title":"Theme document model + theme editor","question":"What does a Theme object contain once every hardcoded color from #t8 is tokenized (plus swim-lane separation px)? How does it relate to the three existing hand-tuned built-in themes and the `ThemeId` union? Does the OKLCH `laneRamp` procedural generator survive becoming hand-editable in a UI? Who wins when two collaborators on the same shared Portfolio pick different themes at once?","blockers":["t2","t8","t11"],"gist":"`{baseId: ThemeId, overrides?: Partial<Theme>}` resolved live (base+override idiom, same as #87's Scenarios) — `ThemeId`/the 3 built-ins stay first-class, hand-editing just patches on top. `laneRamp` stays one atomic `{L,C,startHue}` field edited as a single \"colour family\" control, never exploded into per-lane hex. Two collaborators editing different fields (even a base swap vs. an override) both land, since baseId/each override key are independent Y.Map entries; a literal same-field edit is plain LWW. A single whole-Theme blob field was rejected — proven to silently drop unrelated concurrent edits."},

{"key":"t14","type":"prototype","title":"CRDT decomposition, invariants & derived data","question":"What is the actual CRDT unit — one document per Portfolio, or a subdocument per Program/Scenario? How are referential invariants preserved (today's Zod-checked `laneId` FKs, `dependsOn` edges, `linksToTopLevelMilestone`) once Zod can no longer gate a single write boundary? Who computes the fields the current reducer recomputes-and-persists into state (`isCriticalPath`, cascade output, `rollupHistory`) when N clients could all try to write them concurrently?","blockers":["t4","t11","t13"],"gist":"Program is the subdocument/clock-space boundary (matches #86's one-row-per-Program), not Portfolio and not Scenario — a Scenario is a nested Y.Map inside its Program's own subdoc, correcting #87's \"Scenario subdocument\" phrasing. Zod keeps gating local shape only; referential invariants (laneId/dependsOn/linksToTopLevelMilestone) move to the same live scan-and-surface doctrine #87 used for Scenario orphans, since no single write can be rejected post-merge. isCriticalPath and cascade output stop being persisted — computed live per client like Theme/Scenario, deleting the concurrent-write question rather than answering it. rollupHistory (genuine append-only history) switches from a Y.Array to a date-keyed Y.Map so racing same-day writes converge via per-key LWW instead of duplicating."},
{"key":"t16","type":"grilling","title":"Membership, permissions & unauthenticated access","question":"What roles exist on a shared Portfolio (owner / editor / viewer / public link)? What replaces today's local `wayframe:edit-lock` pseudo-lock, which is a personal view/edit mode toggle, not a real lock, and is meaningless under real multi-user access? Can an unsigned-in visitor to the public showcase create and edit a throwaway Portfolio, and if so, does it live on the same CRDT/DB path or a separate local-only path?","blockers":["t5","t12"],"gist":"Three roles (owner/editor/viewer) held per-identity on a Portfolio, enforced server-side at the Partykit room — not client-side, since a client-owned toggle can never gate its own writes. Public link isn't a fourth role, it's a mechanism handing out viewer or editor to whoever holds it via a lightweight guest session. `wayframe:edit-lock` splits: the access-control half moves server-side and stops being toggleable; the presentation-preference half survives unchanged as a viewer-local setting. An unsigned-in showcase visitor with no invite gets today's existing local-only app untouched — never a hosted row — while a public-link visitor gets a guest session on the real hosted path; graduating a local throwaway Portfolio to hosted reuses #91's migration path rather than inventing a second one."},
{"key":"t17","type":"grilling","title":"Migration of today's artifacts","question":"How do the existing `wayframe:document` localStorage blob, saved `.wayframe.json` files, and the 25 global viewer-preference keys land in the new model? Does `.wayframe.json` survive as a Program-level interchange format, become a full Portfolio export format, or both?","blockers":["t1","t2","t11","t12"],"gist":"Only `wayframe:document` actually needs migrating, since it's the one artifact whose container changes — landed via the same Program-level import routine file-open and CSV/Smartsheet import (#98) already need, triggered at first sign-in (#90) with the signed-in identity becoming owner of a new Portfolio. `.wayframe.json` keeps its existing schema as the Program-level interchange format; a full-Portfolio export is a new, separate container format (`.wayframeportfolio.json`) that nests N of the existing files under shared Portfolio-level fields, not a redesign of the existing one. The 25 viewer-preference keys don't migrate at all — already per-browser, already unscoped, already keyed by globally-unique ids where they touch content at all, so nothing about Portfolios/Programs/multi-user changes what they are."},
{"key":"t19","type":"grilling","title":"Style Override: property set, shape/size vocabulary, precedence ladder","question":"When theme token, document-level default, lane color, category color, status color, per-item Style Override, and viewer preference all have an opinion on one rendered property, what is the exact resolution order? Also lock the property set: marker shapes (star/flag/square/rectangle/circle/diamond, diamond default), marker scale, font scale (0.6x-2.0x), independently-attachable title/date label position (inside/top/right/bottom/left), show/hide, per-item color, phase shape (pill vs. rectangle), and phase size (normal/lean/tall).","blockers":["t2","t7","t18"],"gist":"Most-specific-wins: per-item Style Override > identity-scoped color (category, when the legend-fill viewer mode is on) > Program-level styleDefaults > Theme token > hardcoded fallback. Lane color never enters the ladder at all — every render site uses it only for the lane wash/rail, never marker/pill fill. Status vs. category color aren't two competing rungs, they're what one viewer-preference mode toggle picks between at rung 2, and a per-item override still wins over whichever it picked. Font scale is the one property that composes multiplicatively with the existing global viewer font-scale rather than substituting for it. Marker shape stays a pure editorial choice, never an automatic status encoding — theme.ts already rejected shape-as-status on measured colorblind-contrast grounds."},

{"key":"t15","type":"grilling","title":"Undo model under collaboration","question":"What does \"per-Program undo stack\" (already settled) mean once edits are concurrent and multi-user? Realistically this is per-user × per-Program × possibly per-Scenario origin-scoped CRDT undo, not a single linear stack. What happens to undo on a Scenario switch, and what does undo do for an edit made from the merged All-Programs view spanning multiple Programs at once? Today's model is one linear stack of full-document snapshots shared across every edit source (manual, AI, bulk) — this cannot survive concurrent editing as-is.","blockers":["t13","t14"],"gist":"One Y.UndoManager per (user, Program, active target) — Baseline or one open Scenario — scoped by trackedOrigins to that user's own client id and by shared-type scope to that one Program/Scenario, so a user's undo structurally can't revert a collaborator's edit or reach another Program. Baseline/Scenario undo are already disjoint per #87's own storage shape, so this just builds the manager map that exploits it. Switching Scenarios swaps which target's stack is live, parking (not discarding) the one left behind. The merged All-Programs view needs no stack of its own — every edit still belongs to one Program, so it's a thin local router dispatching Ctrl+Z to the right per-Program manager, not a new kind of stack."},
{"key":"t20","type":"prototype","title":"Lane Rows & vertical allocation","question":"How do explicit Lane Rows (start at 2, grow as needed) coexist with today's automatic duration-pill sub-row stacking, the `use-auto-lane-height` shrink behavior, and `density: normal|lean` plus the new \"tall\" phase size? Is there one allocator or two, and does an item's explicit row assignment override automatic stacking or merely seed it?","blockers":["t11","t19"],"gist":"Two allocators layered, not one: explicit Lane Row bucketing (document content, laneRow ?? 1) is pure grouping with zero collision logic, and the existing stack-intervals.ts greedy stacker still runs unchanged *within* whichever row an item lands in — assignment overrides which row, never turns off automatic collision safety there. Row 1 (a lane's always-present home row) keeps a floor, but a content-derived one: tall-pill height (26) + one real line of title-label text (LABEL_LINE_H=11) + one real line of date-label text (the 12px per-tier gap DATE_TIER_DY already reserves) = 49px, replacing the old flat, content-blind LANE_HEIGHT=132; it still scales past 49 once actual stacked content needs more. Rows 2+ get no such floor — they're opt-in space costing exactly what's stacked into them. Tall phase size composes as a per-sub-row floor, not a multiply-down by lane density, so a tall item still reads tall in a lean lane. Separately, retires the shrink-based \"Auto lane height\" toggle (confirmed the only candidate for a lane-sizing setting) for an expand-only \"fit to screen\": never shrinks below a lane's natural height (over-budget content just scrolls), only stretches lanes into real surplus viewport room — the vertical sibling to #84's horizontal zoom/fit-to-screen. Prototype: branch prototype/lane-rows-94."},
{"key":"t22","type":"grilling","title":"Visibility & legend interaction","question":"Since hiding a swimlane is document content (already decided), what happens live when one collaborator hides a lane on a shared Portfolio mid-review — does everyone else's view change instantly? Does \"hidden\" mean excluded from layout entirely, or laid out but not painted (this changes collision-allocator and export behavior)? Also covers per-category show/hide and one-click category creation from the legend — note the legend today receives no category props at all, despite an existing code comment claiming otherwise; category rendering in the legend must be built from scratch.","blockers":["t2","t16","t19"],"gist":"Lane-hide (`Swimlane.hidden?`) is document content, live-synced like any other CRDT edit (t2's doctrine, no bespoke notification) — and excluded from layout entirely, not just unpainted: filtered out before computeRows, so it reserves no row slot, feeds correctly into #94's fit-to-screen (reclaims real space), and export needs no separate filtering rule since excluded-from-layout is automatically excluded-from-export. Per-category show/hide is the opposite: a viewer preference, not document content — a personal review lens (like ghost/at-risk/critical-path mode), unsynced, since two collaborators plausibly want different category filters live at once. Unlike lane-hide, filtered-out items stay in the shared layout/collision computation for everyone and are merely unpainted for the filtering viewer, so item positions never shift based on anyone's personal filter and cross-viewer references ('that pill in row 2') stay stable. The legend needs building from scratch — confirmed `ChartLegendProps` carries no category fields and the `types.ts` doc comment claiming categories already render there is stale/wrong; one-click creation from the legend should call into `CategoryManager.tsx`'s existing add-category logic rather than reimplement it."},
{"key":"t23","type":"prototype","title":"Unified delta-annotation layer","question":"Design one data shape and one visual vocabulary covering slip (today's `originalDate`), at-risk (`potentialDate`), and the new scenario-diff ghost. Does the new Baseline concept supersede `Milestone.originalDate` entirely? How does the unified layer extend to phases, which have no `originalDate` field at all today? What is the \"smart change labeling\" rule — which deltas earn a visible label when annotation space (quantified in #t9) is scarce?","blockers":["t9","t13","t19"],"gist":"One shape, three kinds: {itemId, kind: slip|at-risk|scenario-diff, field, from, to}. Baseline does NOT supersede originalDate — they answer different questions: Baseline (#85/t11) has no history of its own, it's just the Program's current live data, while originalDate tracks an item's own value slipping from what it used to be. Scenario-diff is a third, orthogonal kind, never persisted — computed live against Baseline's current field value and shown only while a viewer has that Scenario open (viewer-local per #85). Collapses today's two independent layoutGhostBadges passes (#83/t9's finding: slip and at-risk both claim tier 0) into one shared per-item pass. The priority ladder scenario-diff > at-risk > slip IS the tier assignment, not a separate rule — highest priority claims tier 0 and the text label; lower-priority ghosts on the same item still render as unlabeled dashed outlines, escalated outward. Extends to phases via independent originalStartDate/originalEndDate slip fields (a real gap today — phases have no originalDate at all), with potentialDate mapped to endDate. Only 3 tiers exist (GHOST_TIER_DY) — a 4th co-occurring ghost overflows into a count rather than inventing a tier. Retires GhostMode (badge/outline) and AtRiskStyle (sibling/comet/zone) in favor of one shared rendering primitive, color-coded by kind. Prototype: branch prototype/delta-annotation-96."},
{"key":"t35","type":"grilling","title":"AI pipeline: extract into an existing Portfolio's new Program","question":"How does AI extraction target a new Program inside a Portfolio instead of replacing the whole document wholesale, as it does today? How does the \"apply\" step become granular CRDT ops rather than a full-document swap? Do correction ops (`/api/correct`) become Portfolio-scoped, and does the deliberate `PatchOpSchema` prohibition on touching `laneId`/`categoryId` still hold under the new model?","blockers":["t11","t13","t14"],"gist":"Extraction itself gains no portfolio awareness — it still produces one full RoadmapDraft describing one Program's worth of content, exactly as today (a whiteboard photo or CSV never describes more than one Program). Everything changes in apply, not extraction. Per #86's one-row-per-Program storage and #85's portfolioId-on-the-Program-row identity, Portfolio membership is a plain field on a Program's own row, not a separate shared list the Portfolio owns — so a new Program from extraction is just: allocate an id, write one new row {id, portfolioId, order, ...} seeded wholesale from the draft. That write is a legitimate bulk seed, not a swap, since the row is brand new — no prior content, no concurrent editor, no undo history (#93) to clobber — so today's build-the-whole-document-then-write-it-once flow survives almost unchanged; only the target changes, from the app's one global document to one freshly-allocated Program row. The one place apply genuinely touches live shared state is legendCategories, which #85 moved up to the Portfolio precisely because it's shared across every Program in the merged view: if the draft invents categories the Portfolio's legend doesn't have yet, apply must append those as a small additive op, never replace the list wholesale, so it can't stomp categories other Programs already reference. Correction ops don't change at all: they're already Program-scoped (knownIds drawn from one Program's live data, per #89's subdoc boundary), so \"Portfolio-scoped correction ops\" is a category error — `/api/correct` just needs to know which Program's row it's addressing. The laneId/categoryId prohibition holds unchanged, and now implicitly also forbids reassigning an item's Program — same manual-only doctrine, one level up."},
{"key":"t39","type":"grilling","title":"Deterministic import retargeting (CSV/XLSX/Smartsheet)","question":"The non-AI deterministic import paths (spreadsheet wizard, Smartsheet pull) also currently produce a whole replacement document, the same way AI extraction used to. Sibling question to #t35 for the deterministic path: how does a CSV/XLSX/Smartsheet import target an existing or new Program inside a Portfolio, and does it share the merge-vs-replace machinery #t35 designs, or need its own?","blockers":["t11","t13"],"gist":"Premise was already half-wrong: the CSV/XLSX path (SpreadsheetImportTab/rowsToRoadmap) never actually does a whole-document replace — it already diffs rows against the live document and commits {newLanes, adds, updateOps} through one atomic importMerge action, the same granular-ops shape #97 designed for AI extraction. Smartsheet is the one that's actually broken: despite fetchSheetRows returning the identical ParsedRow[] shape rowsToRoadmap consumes, ImportPanel routes it through the 'AI extraction' combined-text tab into /api/extract instead — an expensive, imprecise LLM call standing in for exact deterministic key-matching that already exists. Fix in passing: give Smartsheet its own mapping+rowsToRoadmap path (fold it into SpreadsheetImportTab as a second row source), retiring its AI-extraction routing entirely — its own 'one-way pull, re-pull to sync' framing is exactly rowsToRoadmap's title+lane match-and-diff use case. Portfolio-targeting splits along the same new-vs-existing line #97 drew, but the two import kinds are asymmetric in which case is native to them: extraction is built for 'new Program' (bulk seed, nothing to diff against) and needs adapting to update an existing one; rowsToRoadmap is built for 'existing Program update' (title+lane matching only means something against a live document) and needs only a trivial adaptation for 'new Program' — call it with an empty seed RoadmapData so every row falls through to add, then commit via #97's same allocate-id-and-write-one-Program-row primitive. That primitive is the only thing genuinely shared between the two import kinds; the add-vs-update decision logic (AI reasoning vs. deterministic column-mapped key match) stays separate and doesn't need unifying. Swimlanes are Program-local, not Portfolio-shared (unlike #97's legendCategories carve-out), so a spreadsheet import creating new lanes never needs an additive op against shared Portfolio state — every write lands entirely inside the one target Program's row. updateOps stay plain PatchOps, so #97's Program-scoped correction-ops doctrine carries over unchanged."},
{"key":"t40","type":"grilling","title":"Binary asset storage boundary","question":"Today `companyLogo.dataUrl` and `attachments[].url` are inline base64 data URLs stored directly in the document. Under a hosted DB (#t3) plus a CRDT update log (#t12), inline blobs are a row-size and update-churn problem. Where do binary assets actually live — DB blob column, object storage (S3/R2) with the document holding a reference, or something else — and how does that interact with Snapshot immutability?","blockers":["t3","t12"],"gist":"Same no-blob-store boundary as today, extended rather than replaced: dataUrl stays an inline string field inside the Program's Yjs document, landing in #86's per-Program snapshot-blob row — no DB blob column, no S3/R2, no reference-holding indirection anywhere. The real argument for this isn't just #77's 1GB row headroom (already banked on fitting inline base64), it's the domain Snapshot (capital-S — an immutable whole-Portfolio freeze per CONTEXT.md, not #86's lowercase per-Program compaction blob, easy to conflate): object storage with the document holding a URL reference makes Snapshot immutability expensive two ways — either the referenced object can never be garbage-collected because some past Snapshot might still point at it, or every Snapshot needs its own copy-on-write of every referenced object, new machinery #86 never needed for anything else. Inline storage sidesteps both for free: taking a Snapshot already means copying each Program's current row into a new (snapshot_id, program_id) row, stamped once and never mutated — reusing #86's existing row shape exactly, so a frozen logo or attachment just comes along for the ride with zero asset-specific code. Row-size ceiling needs no new infra, just a sane client-side max-upload-size guardrail, same posture as any other size validation. \"Update-churn\" is overstated: setting a logo/attachment is a rare, user-initiated whole-value write, not per-keystroke churn, and #86's periodic update-log-to-snapshot compaction already absorbs occasional large one-shot blobs same as any other edit. Deliberately deferred: content-hash deduplication of identical assets repeated across many Snapshots of a long-lived Program — a real storage-cost question once Snapshot volume is material, not a correctness one now."},

{"key":"t21","type":"prototype","title":"Swimlane Groups","question":"Design the real container that replaces (or coexists with) today's flat `type: \"separator\"` lane rows: how does a Group own an ordered set of child Swimlanes, what does the 270°-rotated vertical band do to left-gutter width, and what are the ordering/drag semantics when lanes belong to a group?","blockers":["t20"],"gist":"A real container, not an inferred boundary: SwimlaneGroup{id,order,name,color,collapsed} plus a Swimlane.groupId FK, sharing one order space with ungrouped lanes at the top level — the same flat order space separator/lane rows already share, so migration is additive (a separator row becomes a SwimlaneGroup, its trailing lanes get groupId written in). Verified on a fixture with an ungrouped lane sandwiched between two groups: membership survived because it's a real FK, not position-inferred, the way today's separator boundary is. Prototyped three left-gutter/rotation treatments — verdict is the full-width header band (today's separator shape, generalized to real group semantics, zero extra gutter), not a dedicated 270°-rotated rail column. The rotated rail reads well for tall (3+ lane) groups but its textLength-based compression goes illegible once a long name lands in a short 1-2-lane band (a 45-character name squeezed into ~116px of run room); a hybrid thin-spine variant dodges that with a rotate-only-if-tall threshold, but then falls back to a cryptic 3-letter abbreviation chip for exactly the short groups most likely to exist in practice. Treating the 270°-rotated band as an export-time concern (Slides already confirmed native support, #t6) rather than the interactive canvas's sidesteps the tradeoff entirely — the live app never needs it. Collapse is document content (SwimlaneGroup.collapsed, not a viewer preference — same reasoning as Swimlane.density), toggled via onToggleGroupCollapsed. Actually clicking collapse on the rendered prototype (not just reading the code) surfaced a real bug: hiding a group's rows from the row list left every direct milestones-keyed render layer (markers, duration pills, connectors, at-risk projections — all positioned via a laneId→y lookup) orphan-stacked at the top of the chart, because that lookup's not-found fallback was written for \"this shouldn't happen,\" not \"this lane is intentionally hidden.\" Fixed by filtering every such layer to only-visible-lane membership first; a connector's source endpoint needed the same guard independently of its target. Ordering needs no new mechanism: the existing ▲/▼ adjacent-swap already generalizes to crossing a group boundary (reassign groupId) and moving a whole group as a unit (bulk order renumber) — no drag-and-drop library justified. Prototype: branch prototype/swimlane-groups-100."},
{"key":"t24","type":"prototype","title":"Shared tier allocator","question":"How do the four currently-independent layout passes (`layoutDateLabels`, title-layout's tiered title placement, `layoutGhostBadges` consumed twice, `reference-line-layout`) become one budgeted allocator that also serves the new unified delta-annotation layer (#t23) and, for the first time, phases (which have no collision handling today)? Define the starvation policy for when demand exceeds available tiers.","blockers":["t10","t19","t20","t23"],"gist":"One `createZone` function, config'd per call site (tierCount, gap, onExhausted) instead of four bespoke packing loops. The one call site every consumer shares: sort a zone's full demand set by (priority, x), feed it one-by-one into the same greedy left-to-right, escalate-on-collision packer — same-anchor multi-demand ranking (t23's priority ladder) and cross-item/cross-pass collision avoidance (t9's finding) turn out to be the identical mechanism once that's the sort key, no special-case code needed for either. Starvation policy is a zone property, never an item property: `overflow` (lane dates/ghosts, today's unconditional dump tier), `collapse` (delta-annotations, cap past 3 — t23's rule generalized off a hardcoded slice), or `nudge` (reference lines, whose chart-wide row budget can't grow every session the way a lane's vertical room can). Content degradation (dates' full→compact) is tried across every real tier before a zone's policy ever fires — shrinking beats losing position. Titles are the one non-fit: fixed alternating-tier assignment + continuous budget-shrink, not demand-packed, so they keep their own function but call `zone.registerBlocker()` to publish occupancy instead of each consumer hand-building a `blockers` array. PROGRAM-band phases — zero collision handling today — get the exact same overflow-zone config lane ghosts use, closing that gap with no new math. Prototype: branch prototype/shared-tier-allocator-101."},
{"key":"t34","type":"prototype","title":"Milestone editor redesign with visual preview","question":"What does the redesigned milestone editor with a live visual preview actually show, and how does it expose lane row assignment, marker shape, marker scale, per-label (title/date) attachment, font scale, per-item color, category, and show/hide — without becoming an unusable wall of controls?","blockers":["t19","t20","t22"],"gist":"Two-column layout wins: form scrolls on the left (Content always open — title/dates/status/%/owner/category/lane row/comment/critical-path), a live preview pinned on the right at all times. Appearance (markerShape/markerScale/fontScale/titleLabelPosition/dateLabelPosition/color/hidden — #92/t19's locked property set) and Relationships (predecessors/successors/attachments) collapse by default, each badged with a live count so a glance shows what's actually customized without opening anything. Rejected a top-preview-strip + tabs layout: a slim strip has no vertical room to demonstrate a label attached top or bottom, exactly the property the preview most needs to show, and tabs hide Content while editing Appearance where a collapsed section stays inline. The preview renders live per #92/t19's actual resolution ladder (per-item override > category color when the legend-fill toggle is on > theme/program default), with a small source tag under each Appearance control so a user can tell what's really active. phaseShape/phaseSize only appear once an end date is set, matching that ticket's duration-pill-only scoping. Prototype: branch prototype/milestone-editor-redesign-102."},

{"key":"t25","type":"prototype","title":"Connector-line rewrite","question":"What replaces today's 3-waypoint `buildConnectorPath` (elbow/s-curve/rounded, all from the same two endpoints and a midpoint, zero collision avoidance, painted under markers)? How much obstacle avoidance is actually worth building, what geometry does it need as input (post-layout marker and label positions from #t24), and should connectors move above markers in paint order?","blockers":["t24"]},
{"key":"t26","type":"prototype","title":"All-Programs merged view","question":"How do N Programs merge onto one shared time axis (already decided: union of date ranges, program name as a grouping band)? Resolve lane identity and ordering across programs, how the program-name grouping band interacts with Swimlane Groups (#t21) — two banding systems now compete for the same left gutter — per-program vs. Portfolio-level theming in the merged view, and exactly what editing is permitted while viewing it.","blockers":["t15","t21","t24"]},
{"key":"t28","type":"prototype","title":"Deck IR (shared export model)","question":"Design the neutral Deck model (slides/shapes/text as data) that both the PPTX and Google Slides renderers consume, and lock the fidelity floor it guarantees given the capability probes from #t6 and #t7 — the shared model can only promise the intersection of what both formats support, which will be lower than either renderer alone.","blockers":["t6","t7","t18","t19","t24"]},
{"key":"t32","type":"prototype","title":"Outline/tree view","question":"What does the cascaded outline tree actually expose — programs → groups → lanes → rows → milestones/phases/annotations? Which mutations does it permit (reorder, reparent, show/hide)? Does it share `useSelection`'s `Set<string>` selection state with the canvas, so a tree selection and a canvas selection stay in sync?","blockers":["t20","t21","t22"]},

{"key":"t27","type":"grilling","title":"Cross-program executive rollup","question":"How does RAG status roll up across Programs for a Portfolio-level executive view? What does the trend arrow mean when `rollupHistory` is currently per-lane and per-program? What happens to that already-unbounded, append-only log under daily writes from multiple concurrent collaborators?","blockers":["t14","t26"]},
{"key":"t29","type":"grilling","title":"Export scope, selection & viewport determinism","question":"What does the export dialog let a user choose: which Program(s), Baseline vs. which Scenario, the combined merged deck, the executive view? Which viewport/zoom level does the export render at, given zoom (#t10) may end up viewer-local state rather than document content? Also fixes the known live bug where the off-screen Executive slide capture is passed no `timelineSummary` prop, silently omitting the executive timeline strip whenever exporting from Program view.","blockers":["t10","t13","t26","t28"]},
{"key":"t30","type":"grilling","title":"Google Slides delivery & consent","question":"What is the incremental OAuth scope request flow for Slides/Drive access, where does the generated deck land in the user's Drive, where is the refresh token stored in the hosted DB, and what is the failure/retry UX when a user is signed in but has never granted Slides scope?","blockers":["t5","t16","t28"]},
{"key":"t31","type":"grilling","title":"Snapshot model","question":"What exactly does a Snapshot freeze: a materialized render-ready Portfolio state, a rendered Deck, or a raw CRDT state vector? How does a Snapshot stay readable years after the schema (#t1) has moved on without violating its own immutability by migrating on read? Does it need to capture viewer-side state too — a \"view-only forever\" record that renders differently depending on who's viewing it is not actually a record.","blockers":["t1","t12","t13","t18","t28"]},
{"key":"t33","type":"grilling","title":"Mass-edit generalization","question":"What replaces today's closed 4-operation `BulkEditOp` union (shift/status/lane/acceptBaseline), each with its own hand-written builder, preview branch, and toolbar entry? Design a generic field-patch operation set covering Style Override properties, Lane Row (re)assignment, delete, font scale, and selection across multiple Programs at once.","blockers":["t15","t19","t32"]},
{"key":"t36","type":"prototype","title":"Presence UX design","question":"Given #t4 selects the awareness/presence API, what does presence actually look like on a swimlane chart: remote cursors, colored avatars, remote selection highlighting, follow-mode? This was explicitly named as part of the realtime-collaboration decision but has no design of its own yet.","blockers":["t4"],"gist":"Avatar stack + colored remote-selection rings (reusing the real local selection-ring mechanism, parameterized per peer) as the core design. Live cursors deferred/optional — a mouse position over an SVG chart isn't as meaningful as a text cursor. Follow-mode is valid future work, not bundled into the initial cut. Open question: two peers selecting the same milestone at once."},
{"key":"t37","type":"grilling","title":"Invite / share flow","question":"Given the role model from #t16 (owner/editor/viewer/public link), design the actual sharing mechanism: email invite, shareable link, domain-restricted access, or some combination — and what a recipient sees on first open of a shared Portfolio.","blockers":["t16"]},
{"key":"t38","type":"grilling","title":"Offline, reconnect & conflict UX","question":"What does a collaborator see when the realtime socket drops mid-edit? Is offline editing supported at all, and if so, how does a reconnecting client's queued local changes merge back into a CRDT document (#t14) that has moved on without it?","blockers":["t4","t14"]},
{"key":"t41","type":"grilling","title":"Performance budget & virtualization strategy","question":"The renderer is already a 2679-line component rendering a full SVG timeline with no windowing; the All-Programs merged view (#t26) multiplies that by N programs on one axis, and `rollupHistory` grows unbounded. Set an explicit performance budget and decide whether/where virtualization is needed (lane rows, off-screen time ranges, or both) before it becomes a retrofit.","blockers":["t11","t26"]},
{"key":"t42","type":"grilling","title":"Multi-client CRDT test strategy","question":"How does the team verify CRDT convergence, the Scenario-rebase rules from #t13, and presence behavior under concurrent multi-client edits? Decide the testing approach (multi-client simulation harness, property-based convergence tests, manual multi-browser protocol, or a combination) before any collaborative feature ships untested.","blockers":["t14"]}
]
